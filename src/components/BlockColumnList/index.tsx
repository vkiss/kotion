import { FC, memo } from 'react';

interface Props {
  data: any,
}

// ::
const BlockColumnList: FC<Props> = ({ data }) => {
  // console.log(data);

  return null;
};

export default memo(BlockColumnList);

